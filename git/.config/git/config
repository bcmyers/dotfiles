# Default

[alias]
    pulls = !git pull --rebase origin master && git submodule update --init --recursive
    sub = !git submodule update --init --recursive
[core]
    editor = nvim
    excludesfile = /Users/bcmyers/.config/git/ignore
    fsmonitor = rs-git-fsmonitor
[commit]
    gpgsign = true
[credential]
    helper = ""
    helper = manager-core
[credential "https://dev.azure.com"]
    useHttpPath = true
[diff]
    algorithm = patience
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
[github]
    user = bcmyers
[init]
    defaultBranch = main
[merge]
    tool = vimdiff
[mergetool]
    path = nvim
[pull]
    ff = only
    rebase = true
[push]
    default = simple
[user]
    name = Brian Myers
    email = brian.carl.myers@gmail.com
    signingkey = B86678B99457460F

# Robinhood

[includeIf "gitdir:~/robinhood/"]
    path = ~/.config/git/robinhood

