# Legacy web-server configuration

These files preserve the last manually managed nginx and Certbot configuration
for `bcmyers.com`. They are not imported by the Nix flake and are not deployed
to either the Mac or ThinkPad.

Treat them as migration inputs for a future declarative server configuration,
not as a current deployment mechanism. Review their TLS and application
settings before reusing them.
